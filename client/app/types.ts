export type Appreciation = {
  senderName: string,
  receiverName: string,
  comment: string,
}

export type User = {
  email?: string,
  firstName?: string,
  lastName?: string,
}

export type ImageUrl = {
  url: string
}